# Excel 导入 · 格式说明（待对齐）

当前无法直接读取 `.xlsx` 二进制文件。要对照你在 Excel 里的记账方式并设计导入逻辑，需要先了解表格结构。

请任选一种方式提供信息：

1. **导出为 CSV**  
   在 Excel 中把「2024 finance tracking - 副本」的**记账表**另存为 CSV（如 `2024-tracking.csv`），放到项目下或发路径，我可以按列名和样例设计导入。

2. **文字描述**  
   说明：
   - 有哪些 **Sheet 名称**（如：资产总览、流水、月度汇总…）
   - 每个 Sheet 的 **列名**（第一行）
   - 每一行通常记什么（例如：一行=一笔买入/卖出，或一行=某日某账户余额）
   - 若有「大类/细分/风险」等，列名和取值是什么

3. **截图或复制表头+几行示例**  
   把表头行和 1～2 行示例数据粘贴到对话里（或截图），我根据实际列名和格式设计导入与字段映射。

设计导入时会尽量保持与你现有 Excel 的列和含义一致，必要时再微调数据模型或界面。
