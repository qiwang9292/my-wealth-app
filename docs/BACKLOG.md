# 后续工作计划（BACKLOG）

本文档与当前版本同步，用于和 AI 或团队一起排期、拆任务。完成一项后可勾选或移走，新想法随时追加。

**主线顺序**：Phase 1（基础框架）→ Phase 2（真实数据接入与持续记录）→ Phase 3（LLM 与智能分析，暂缓）。

---

## 任务怎么称呼（沟通约定）

- 每个 Phase 下的条目按 **BUG** → **约定** → **功能** → **UI** 四类归类（与原先 backlog 习惯一致）。
- 每条任务有一个**名称**（下列表中加粗部分），沟通时可说：  
  **`Phase 2 · 功能 · 导入自动瞬间`**，或直接说名称 **「导入自动瞬间」**（必要时加 Phase 区分）。

---

## 模块命名（产品界面用语）

| 名称 | 含义 |
|------|------|
| **资产表** | 列出每个产品内容的表格（主页面上方大表） |
| **dashboard** | 汇总总资产的区域（主页面底部汇总块） |
| **功能区** | 汇集「新增产品、**− 删减产品**、**标记已清仓**、记一笔、流水列表、**已清仓产品**（`/closed-products`）、**刷新权益净值**、**刷新全部净值**、更新净值、拍瞬间、瞬间对比、产品详情」等；**底栏两行**：上行主操作，下行右对齐「放弃/保存表格修改」 |
| **瞬间** | 按当前份额与净值生成的资产定格，用于月末对比等（代码中仍用 snapshot，仅面向用户的文案用「瞬间」） |
| **产品详情** | `/products` 列表与 `/products/[id]` 详情页：基本信息、手填覆盖说明、该产品流水、最近净值（与流水列表分开展示） |

---

## Phase 1：基础框架（已基本完成）

**目标**：可维护产品、看总览、记流水、手填/刷新净值、拍瞬间与对比、清仓与删减规则、大类展示与汇率等约定落地。

### BUG

- [x] **手改份额回跳零** — 手动输入份额或成本后无返回、内容跳回 0（已修：保存后等待 overview 再关编辑态，PATCH 与回显一致）。
- [x] **总览白板** — `categoryList` / `products` 非数组或缺字段导致整页白板（已做兜底；overview 内美元日元归并、汇率失败不阻断整包）。
- [x] **Excel 导入构建失败** — `exceljs` `load` 与 `Buffer` 类型不兼容（已用窄化签名调用）。
- [x] **Next 包损坏** — 升级后 `Cannot find module '../cli/next-test.js'`（已重装 `next@16.2.1`）。
- [x] **构建采集 category-targets 失败** — 升级后缓存态异常（清理 `.next` 后恢复）。

### 约定

- [x] **份额成本锁定** — 无流水可 `unitsOverride` / `costOverride`；有流水只读、PATCH 拒绝覆盖。见 `docs/units-cost-rules.md`。
- [x] **快照改瞬间文案** — 面向用户用「瞬间」；代码里保留 `snapshot`。
- [x] **四大类与现金汇率** — 现金/债权/商品/权益、`categories.ts`、美元日元 Frankfurter/ECB、overview 归并等规则。

### 功能

- [x] **手填覆盖可见** — 覆盖存 Product；产品详情单独说明，与流水分块。
- [x] **产品详情与流水** — `/products`、`/products/[id]`，与总览、流水列表互链。
- [x] **表格草稿与离开确认** — 份额/总成本本地草稿、「保存表格修改」、未保存离开提示。
- [x] **按类刷新净值** — `POST /api/refresh-prices` 支持 `category`；功能区「刷新权益」「刷新全部」。
- [x] **净值日期悬停** — 资产表「净值/汇率」列 `title` 展示记录日期。
- [x] **删减与清仓** — 软删/清仓 API、已清仓页、`monthRealizedPnl` 与「本月盈亏（持仓）」拆分。
- [x] **产品信息纠偏** — 详情页可改大类/细分/账户/类型/代码；`PATCH` 白名单。

### UI

- [x] **代码列紧凑** — 单行「查代码」；查不到弹窗手输代码。

---

## Phase 2：真实基线导入与持续记录（已落地）

**目标**：Excel 导入真实基线 → 预检确认后落库 → 日常「记一笔」；**导入成功自动拍一条瞬间**；长期 **每月最后一个交易日自动月末瞬间**。

### 就绪速查

| 能力 | ready | 说明 |
|------|-------|------|
| 总览 / 资产表 / dashboard | ✅ | 日常看持仓与大类 |
| 新增产品、详情纠偏 | ✅ | 导入后可改 |
| 记一笔、流水列表 | ✅ | 持续流水 |
| 份额成本锁定 | ✅ | 见 `units-cost-rules.md` |
| 更新净值 / 刷新净值 | ✅ | 非标手填 |
| 手拍瞬间、瞬间对比 | ✅ | 功能区入口 |
| Excel 预检 + 三分栏 + 错误导出 + 模板 | ✅ | 见 `docs/excel-import-format.md` |
| 导入成功自动瞬间 | ✅ | `persistSnapshot` + 与总览一致估值 |
| 月末交易日自动瞬间 | ✅ | `GET /api/cron/month-end-snapshot` + `src/lib/cn-trading-calendar.ts` |
| 定时刷净值（Cron） | ✅ | `GET /api/cron/refresh-prices`；需配置 `CRON_SECRET`，部署见根目录 `vercel.json` |

### BUG

- （暂无登记；新发现请在本节追加并加 **名称**。）

### 约定

- [x] **导入幂等规则** — 已写入 `docs/excel-import-format.md`（匹配键、更新/新增/跳过、代码冲突）。

### 功能

- [x] **Excel 预检再导入** — 先「先预检」再「开始导入」；`.xlsx`/`.csv`。
- [x] **Excel 预览三分栏** — 将新增 / 将更新 / 将跳过 + 原因；解析错误可导出 CSV；模板 `GET /api/import-excel/template`。
- [x] **导入自动瞬间** — 导入写库成功且至少一行新增/更新后自动拍瞬间；备注 `Excel 导入自动生成`；估值与 `/api/overview` 对齐（`src/lib/valuation-snapshot.ts`）。
- [x] **月末交易日瞬间** — 中国时区「今天」= 当月最后 A 股交易日时拍瞬间；备注 `月末自动·YYYY-MM`；同月幂等；休市日表见 `cn-trading-calendar.ts`（每年宜核对交易所公告补全）。
- [x] **定时刷净值** — `GET /api/cron/refresh-prices` 全库 FUND/STOCK；`Authorization: Bearer CRON_SECRET`。
- [x] **查代码备用源** — 东方财富无结果时用更短关键词再搜；接口 404/500 返回 `hint`；弹窗展示引导手动输入。

### UI

- [x] **导入结果反馈** — 弹窗展示三分栏摘要、错误导出、成功提示含自动瞬间说明。

---

## Phase 3：LLM 与智能分析（暂缓）

**目标**：在 Phase 2 数据稳定后，用 overview / 瞬间 / 流水做解读与对比（非投资建议；需合规话术与失败兜底）。

### BUG

- （上线 LLM 后登记：超时、空响应、解析失败等。）

### 约定

- [ ] **LLM 工程约束** — 数据最小化与脱敏；异步 + 缓存；展示「依据摘要」与生成时间；失败回退规则模板。
- [ ] **合规话术** — 统一「仅供参考，不构成投资建议」；区间表述、避免单点收益承诺。

### 功能

- [ ] **大类占比 Scenario** — 输入：大类占比 + 风险偏好 + 周期；输出：解读、2～3 套 scenario（区间 + 年化表述 + reasoning）、波动提示。
- [ ] **双瞬间 AI 对比** — 输入：两瞬间总资产/大类/产品变动/区间盈亏；输出：摘要、优缺点、≤3 条建议。接口：`POST /api/ai/snapshot-compare`（异步）+ `GET .../{jobId}`；缓存键含 `promptVersion`。
- [ ] **单产品 AI 复盘** — 净值 + 流水 + 时长；表现分解、操作习惯、风险与改进；不做价格预测。

### UI

- [ ] **瞬间对比 AI 卡片** — 简版/详版；字段：基础信息、变化摘要、`pros[]`/`cons[]`/`actions[]`。
- [ ] **双瞬间 AI 验收** — 可触发、失败可读、短期缓存、默认 brief；测：正常 / 瞬间缺失 / 超时 / 多次一致性。

---

## 跨阶段 · 可选（仍按四类归类，便于挂进某 Phase）

### BUG

- （暂无。）

### 约定

- [ ] **overview 归并策略** — 当前打开总览会写库归并美元/日元；若改「只展示不写库」需另方案。

### 功能

- [ ] **大类独立刷净值** — 债权、商品等 FUND/STOCK 独立按钮（复用 `category`）。
- [ ] **流水筛选导出** — 流水列表按类型筛选、导出 CSV。
- [ ] **瞬间大类对比导出** — 瞬间对比按大类汇总、导出。
- [ ] **多瞬间导出 Excel** — 多选瞬间导出 `.xlsx`（汇总 / 明细 / 差异）。
- [ ] **独立产品编辑页** — 总览外编辑；删/清仓与现有 API 对齐。
- [ ] **多账户/多组合视图** — 按组合或账户看总资产与占比。
- [ ] **手填覆盖审计** — 与流水分开的变更记录。
- [ ] **汇率数据源** — 标注时间或换更接近购汇价的源。

### UI

- [ ] **dashboard 空间利用** — 底部汇总区信息密度与布局优化。

---

## 使用方式

1. **排期**：Phase 2 已闭合；下一迭代以 **Phase 3** 或 **跨阶段可选** 为主。定时任务需在环境变量配置 `CRON_SECRET`，Vercel 部署使用根目录 `vercel.json`（其他平台请用同等调度请求带 Bearer）。  
2. **沟通**：使用 **`Phase N · 分类 · 名称`**（名称即各条加粗词）。  
3. **与 AI 协作**：带上当前要做的 **名称** + 本文档 + `PROJECT-PROGRESS.md` + 模块命名（资产表 / dashboard 等）。  
4. **更新**：完成后勾选；新任务补全 **名称** 再放入对应 Phase 与分类。
